function vote (){
    var name = document.getelementbyid("name").value:
    var pass = 100* math.random();
    var result = document.getelementbyid("result");
    result.innerHTML= name +  "you have scored :  "
    
    if( marks >=0 && <=35)
    {
        resultmessage= name + "failed"+ marks +("marks");

    }
    if( marks >=60 && <=80)
        {
            resultmessage= name + "he has passed"+ marks +("marks");
    
        }
        if( marks >=0 && <=35)
            {
                resultmessage= name + ""+ marks +("marks");
        
            }
    }
    